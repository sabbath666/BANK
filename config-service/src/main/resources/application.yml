spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations:
            - 'classpath:/config/usersservice'
            - 'classpath:/config/sd-service'
server:
  port: 8888
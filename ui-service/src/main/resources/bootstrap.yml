spring:
  application:
    name: ui-service
  profiles:
    active: dev

#  cloud:
#    consul:
#      discovery:
#        instance-id: ${spring.application.name}:${random.uuid}

logging:
  config: 'classpath:log4j2.yml'


